# Network to use: localnet | testnet | mainnet
NETWORK=testnet

# Sui private key (bech32 format: suiprivkey1...)
# Generate with: sui keytool generate ed25519
# Or export from wallet: sui keytool export --key-identity <address>
PRIVATE_KEY=suiprivkey1...

# Optional: override the default RPC for your chosen network
# Defaults are set in src/config.ts — override here if needed
# RPC_URL=https://sui-testnet-rpc.publicnode.com

# IKA coin object ID (required for mainnet/testnet DKG and signing)
# Get IKA testnet tokens at https://faucet.ika.xyz/ (swap SUI → IKA)
# Then find your IKA coin object: sui client objects --json | grep IKA
# IKA_COIN_OBJECT_ID=0x...
